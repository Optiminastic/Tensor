# pnpm runs scripts through cmd.exe on Windows, which cannot parse the POSIX
# `VAR=value cmd` prefix used by the dev/build/start/analyze scripts. The shell
# emulator makes pnpm parse those scripts itself, so they behave identically on
# Windows and POSIX shells.
shell-emulator=true
